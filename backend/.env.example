# Server configuration
PORT=3000
NODE_ENV=development

# Google Auth 
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# M1 server information
OWNER_FIRST_NAME=YourFirstName
OWNER_LAST_NAME=YourLastName
SERVER_PUBLIC_IP=127.0.0.1

# Course live-update stream
COURSE_WEBSOCKET_URL=wss://8.229.22.124

# Leave blank for local HTTP development. Set both paths on the cloud VM to
# serve HTTPS; use PORT=443 in that production configuration.
TLS_CERT_PATH=
TLS_KEY_PATH=

# Other API keys, secrets, and configurations
